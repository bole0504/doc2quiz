# Phase 2 backlog

Items intentionally deferred from Phase 1:

- **Admin dashboard**: full KPI (attempts/users), activity feed, charts
- **`/admin/users`**: CRUD, invite editors
- **Register**: `/register` (web) + `MobRegister` (mobile) + API
- **Mobile**: Home screen, 4-tab bar (Trang chủ / Bộ đề / Lịch sử / Tài khoản), local resume
- **User theme**: `themePalette`, `themeDark` on `User` model + settings UI
- **Multi-answer questions**: schema, parser, mobile `variant=multi`
- **Admin responsive**: sidebar collapse / hamburger below 960px
- **Assets**: result illustrations, production logo
